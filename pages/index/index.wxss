/* 游戏中心首页样式 */
.game-container {
  background: #f5f5f5;
  min-height: 100vh;
}

/* 轮播图区域 */
.carousel-section {
  margin-bottom: 20rpx;
}

.game-swiper {
  height: 459rpx;
  width: 100%;
}

.carousel-image {
  width: 100%;
  height: 100%;
}

/* 功能入口区域 */
.entry-section {
  background: #fff;
  margin: 0 20rpx 20rpx;
  border-radius: 16rpx;
  box-shadow: 0 2rpx 20rpx rgba(0, 0, 0, 0.1);
  padding: 30rpx 10rpx 20rpx;
}

.entry-grid {
  display: flex;
  justify-content: space-around;
  align-items: center;
}

.entry-item {
  display: flex;
  flex-direction: column;
  align-items: center;
  padding: 10rpx;
  border-radius: 12rpx;
  transition: all 0.3s ease;
}

.entry-item:active {
  background: #f0f0f0;
  transform: scale(0.95);
}

.entry-icon {
  display: flex;
  align-items: center;
  justify-content: center;
  box-shadow: 0 4rpx 12rpx rgba(0, 0, 0, 0.1);
}

.entry-icon.category-icon {
  width: 320rpx;
  height: 380rpx;
  border-radius: 16rpx;
  background: #fff;
  border: 2rpx solid #f0f0f0;
  overflow: hidden;
  margin-bottom: 16rpx;
}

.category-image {
  width: 100%;
  height: 100%;
  border-radius: 12rpx;
}

.entry-icon.all-categories {
  width: 320rpx;
  height: 380rpx;
  background: linear-gradient(135deg, #666 0%, #999 100%);
  border-radius: 16rpx;
  margin-bottom: 16rpx;
}

.entry-title {
  font-size: 32rpx;
  color: #333;
  font-weight: 600;
  text-align: center;
  line-height: 1.2;
}

/* 充值规则区域 */
.recharge-rules-section {
  background: #fff;
  margin: 0 20rpx 20rpx;
  border-radius: 16rpx;
  box-shadow: 0 2rpx 20rpx rgba(0, 0, 0, 0.1);
  overflow: hidden;
}

.recharge-rules-grid {
  display: flex;
  flex-direction: column;
  gap: 20rpx;
  padding: 30rpx;
}

.recharge-rule-item {
  background: linear-gradient(135deg, #fff 0%, #fafafa 100%);
  border: 2rpx solid #f0f0f0;
  border-radius: 16rpx;
  padding: 24rpx 20rpx;
  transition: all 0.3s ease;
  position: relative;
  overflow: hidden;
  display: flex;
  flex-direction: row;
  align-items: center;
  justify-content: space-between;
  height: auto;
  min-height: 120rpx;
  box-shadow: 0 4rpx 20rpx rgba(0, 0, 0, 0.08);
}

.recharge-rule-item:active {
  transform: translateY(-2rpx);
  box-shadow: 0 8rpx 30rpx rgba(255, 107, 0, 0.2);
  border-color: #FF6B00;
  background: linear-gradient(135deg, #fefefe 0%, #f8f8f8 100%);
}

.recharge-rule-item::before {
  content: '';
  position: absolute;
  top: 0;
  left: 0;
  right: 0;
  height: 4rpx;
  background: linear-gradient(90deg, #FF6B00 0%, #FF8C00 100%);
}

/* 左侧充值金额 */
.rule-left {
  display: flex;
  flex-direction: column;
  align-items: center;
  justify-content: center;
  min-width: 110rpx;
  margin-right: 10rpx;
  background: linear-gradient(135deg, #F5F7FA 0%, #EEF2F7 100%);
  padding: 16rpx 12rpx;
  border-radius: 10rpx;
  border: 2rpx solid #E1E8ED;
}

.rule-label {
  font-size: 20rpx;
  color: #8B96A5;
  margin-bottom: 6rpx;
  line-height: 1;
  font-weight: 500;
}

.rule-amount {
  font-size: 36rpx;
  font-weight: 700;
  color: #4A90E2;
  line-height: 1;
}

/* 中间赠送区域 */
.rule-center {
  flex: 1;
  display: flex;
  flex-direction: row;
  align-items: center;
  justify-content: center;
  margin-right: 8rpx;
  gap: 8rpx;
}

.rule-arrow {
  display: flex;
  align-items: center;
  justify-content: center;
  width: 44rpx;
  height: 44rpx;
  background: linear-gradient(135deg, #FF6B00 0%, #FF8C00 100%);
  border-radius: 50%;
  box-shadow: 0 3rpx 10rpx rgba(255, 107, 0, 0.3);
  border: 2rpx solid rgba(255, 255, 255, 0.5);
  flex-shrink: 0;
}

.arrow-icon {
  font-size: 28rpx;
  font-weight: 800;
  color: #fff;
  line-height: 1;
  text-shadow: 0 2rpx 4rpx rgba(0, 0, 0, 0.2);
}

.rule-send-big {
  display: flex;
  flex-direction: column;
  align-items: center;
  justify-content: center;
  background: linear-gradient(135deg, #FF6B00 0%, #FF8C00 100%);
  padding: 14rpx 16rpx;
  border-radius: 10rpx;
  box-shadow: 0 4rpx 12rpx rgba(255, 107, 0, 0.4);
  min-width: 90rpx;
  border: 2rpx solid rgba(255, 255, 255, 0.3);
  flex-shrink: 0;
}

.send-label {
  font-size: 18rpx;
  color: #fff;
  margin-bottom: 6rpx;
  line-height: 1;
  font-weight: 600;
  letter-spacing: 0.5rpx;
}

.send-amount {
  font-size: 32rpx;
  font-weight: 800;
  color: #fff;
  line-height: 1;
  text-shadow: 0 2rpx 6rpx rgba(0, 0, 0, 0.2);
}

.no-gift {
  display: flex;
  align-items: center;
  justify-content: center;
  width: 44rpx;
  height: 44rpx;
  background: linear-gradient(135deg, #8B96A5 0%, #6B7785 100%);
  border-radius: 50%;
  box-shadow: 0 3rpx 10rpx rgba(139, 150, 165, 0.3);
  border: 2rpx solid rgba(255, 255, 255, 0.5);
  flex-shrink: 0;
}

/* 等号 */
.equal-sign {
  display: flex;
  align-items: center;
  justify-content: center;
  margin-right: 8rpx;
  flex-shrink: 0;
}

.equal-icon {
  font-size: 32rpx;
  font-weight: 800;
  color: #8B96A5;
  line-height: 1;
}

/* 右侧总金额区域 */
.rule-right {
  display: flex;
  flex-direction: column;
  align-items: center;
  justify-content: center;
  min-width: 110rpx;
  background: linear-gradient(135deg, #FFF9F0 0%, #FFF3E0 100%);
  padding: 16rpx 12rpx;
  border-radius: 10rpx;
  border: 2rpx solid #FFE0B2;
  flex-shrink: 0;
}

.total-section {
  display: flex;
  flex-direction: column;
  align-items: center;
  gap: 6rpx;
}

.total-label {
  font-size: 20rpx;
  color: #8B6914;
  font-weight: 600;
  line-height: 1;
}

.total-amount {
  font-size: 36rpx;
  font-weight: 800;
  color: #FF6B00;
  line-height: 1;
  text-shadow: 0 2rpx 4rpx rgba(255, 107, 0, 0.15);
}

/* 响应式调整 - 充值规则 */
@media (max-width: 750rpx) {
  .recharge-rules-grid {
    gap: 15rpx;
    padding: 20rpx;
  }
  
  .recharge-rule-item {
    padding: 20rpx 16rpx;
    min-height: 110rpx;
  }
  
  .rule-amount {
    font-size: 32rpx;
  }
  
  .send-amount {
    font-size: 28rpx;
  }
  
  .total-amount {
    font-size: 32rpx;
  }
  
  .rule-left {
    min-width: 95rpx;
    margin-right: 8rpx;
    padding: 14rpx 10rpx;
  }
  
  .rule-center {
    margin-right: 6rpx;
    gap: 6rpx;
  }
  
  .rule-arrow {
    width: 40rpx;
    height: 40rpx;
  }
  
  .arrow-icon {
    font-size: 24rpx;
  }
  
  .rule-send-big {
    min-width: 80rpx;
    padding: 12rpx 14rpx;
  }
  
  .equal-sign {
    margin-right: 6rpx;
  }
  
  .equal-icon {
    font-size: 28rpx;
  }
  
  .rule-right {
    min-width: 95rpx;
    padding: 14rpx 10rpx;
  }
}

/* 会员充值区域 */
.membership-section {
  background: #fff;
  margin: 0 20rpx;
  border-radius: 16rpx;
  box-shadow: 0 2rpx 20rpx rgba(0, 0, 0, 0.1);
  overflow: hidden;
}

.section-header {
  padding: 30rpx 30rpx 20rpx;
  border-bottom: 1rpx solid #f0f0f0;
  display: flex;
  justify-content: space-between;
  align-items: center;
}

.section-title {
  display: flex;
  align-items: center;
  font-size: 32rpx;
  font-weight: 600;
  color: #333;
}

.section-title text {
  margin-left: 12rpx;
}

.refresh-btn {
  padding: 8rpx;
  border-radius: 50%;
  transition: all 0.3s ease;
}

.refresh-btn:active {
  background: #f0f0f0;
  transform: rotate(180deg);
}

/* 加载状态 */
.loading-container {
  padding: 60rpx;
  text-align: center;
}

/* 空状态 */
.empty-container {
  padding: 60rpx;
}

/* 套餐网格 */
.membership-grid {
  display: flex;
  flex-direction: column;
  gap: 20rpx;
  padding: 30rpx;
}

.package-item {
  background: linear-gradient(135deg, #fff 0%, #fafafa 100%);
  border: 2rpx solid #f0f0f0;
  border-radius: 16rpx;
  padding: 30rpx;
  transition: all 0.3s ease;
  position: relative;
  overflow: hidden;
  display: flex;
  flex-direction: row;
  align-items: center;
  justify-content: space-between;
  height: auto;
  min-height: 140rpx;
  box-shadow: 0 4rpx 20rpx rgba(0, 0, 0, 0.08);
}

.package-item:active {
  transform: translateY(-2rpx);
  box-shadow: 0 8rpx 30rpx rgba(230, 67, 64, 0.15);
  border-color: #e64340;
  background: linear-gradient(135deg, #fefefe 0%, #f8f8f8 100%);
}

.package-item::before {
  content: '';
  position: absolute;
  top: 0;
  left: 0;
  right: 0;
  height: 4rpx;
  background: linear-gradient(90deg, #e64340 0%, #ff8a80 100%);
}

/* 左侧商品信息 */
.package-left {
  flex: 1;
  display: flex;
  flex-direction: column;
  justify-content: center;
  margin-right: 20rpx;
}

.package-title {
  font-size: 32rpx;
  font-weight: 600;
  color: #333;
  margin-bottom: 8rpx;
  line-height: 1.4;
  display: -webkit-box;
  -webkit-line-clamp: 1;
  -webkit-box-orient: vertical;
  overflow: hidden;
}

.package-description {
  font-size: 24rpx;
  color: #666;
  margin-bottom: 8rpx;
  line-height: 1.4;
  display: -webkit-box;
  -webkit-line-clamp: 1;
  -webkit-box-orient: vertical;
  overflow: hidden;
}

.package-tags {
  margin-top: 0;
}

.tag {
  font-size: 20rpx;
  color: #e64340;
  background: #fff5f5;
  padding: 4rpx 8rpx;
  border-radius: 8rpx;
  display: inline-block;
}


/* 中间价格区域 */
.package-center {
  display: flex;
  flex-direction: column;
  align-items: center;
  justify-content: center;
  min-width: 150rpx;
  margin-right: 20rpx;
  gap: 8rpx;
}

/* 价格容器 */
.price-container {
  display: flex;
  flex-direction: column;
  align-items: center;
  gap: 4rpx;
}

/* 会员价标签（现在显示价格） */
.member-price-label {
  font-size: 36rpx;
  font-weight: 700;
  color: #e64340;
  background: transparent;
  padding: 0;
  border: none;
  line-height: 1;
  text-align: center;
}

/* 原价 */
.original-price {
  font-size: 22rpx;
  color: #999;
  text-decoration: line-through;
  text-align: center;
  line-height: 1;
}

.discount {
  font-size: 20rpx;
  color: #fff;
  background: #e64340;
  padding: 4rpx 8rpx;
  border-radius: 8rpx;
  display: inline-block;
}

/* 右侧按钮区域 */
.package-right {
  display: flex;
  align-items: center;
  justify-content: center;
  min-width: 120rpx;
}

/* 响应式调整 */
@media (max-width: 750rpx) {
  .membership-grid {
    gap: 15rpx;
    padding: 20rpx;
  }
  
  .package-item {
    padding: 25rpx;
    min-height: 120rpx;
    flex-direction: row;
  }
  
  .package-title {
    font-size: 30rpx;
  }
  
  .member-price-label {
    font-size: 34rpx;
  }
  
  .package-left {
    margin-right: 15rpx;
  }
  
  .package-center {
    min-width: 120rpx;
    margin-right: 15rpx;
  }
  
  .package-right {
    min-width: 100rpx;
  }
}

/* 动画效果 */
.package-item {
  animation: slideUp 0.6s ease-out forwards;
}

.package-item:nth-child(1) { animation-delay: 0.1s; }
.package-item:nth-child(2) { animation-delay: 0.2s; }
.package-item:nth-child(3) { animation-delay: 0.3s; }
.package-item:nth-child(4) { animation-delay: 0.4s; }
.package-item:nth-child(5) { animation-delay: 0.5s; }
.package-item:nth-child(6) { animation-delay: 0.6s; }

@keyframes slideUp {
  from {
    opacity: 0;
    transform: translateY(30rpx);
  }
  to {
    opacity: 1;
    transform: translateY(0);
  }
}